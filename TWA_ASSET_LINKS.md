# تشغيل تطبيق أندرويد بدون شريط المتصفح

فحصت ملف `downloads/client/AlazharCity.apk`: هو Trusted Web Activity للحزمة `io.github.amh012_hu.twa` وموجّه إلى `https://amh012-hu.github.io/AlazharCityDelivery/`.

سبب ظهور شريط Chrome في التطبيق أن ملف Digital Asset Links غير موجود على أصل الموقع؛ الفحص الحالي لـ `https://amh012-hu.github.io/.well-known/assetlinks.json` يرجع `404`. عند فشل التحقق، يفتح Chrome الصفحة بوضع التبويب المخصص ويظهر الشريط. فتح رابط الموقع يدويًا في تبويب سيظل يعرض واجهة المتصفح دائمًا.

الملف `TWA_ASSET_LINKS.json` يحتوي اسم الحزمة وبصمة شهادة APK الموجودة هنا. لكي يخفي التطبيق شريط المتصفح:

1. انشر محتوى الملف بعنوان الأصل بالضبط: `https://amh012-hu.github.io/.well-known/assetlinks.json`، مع استجابة `200` و`Content-Type: application/json`.
2. مشروع GitHub Pages الحالي منشور داخل المسار `/AlazharCityDelivery/`؛ وضع الملف داخل هذا المستودع وحده سينشئ رابطًا داخل المسار، وليس الرابط الجذري الذي يتحقق منه Chrome. يجب نشره من مستودع الموقع الجذري `amh012-hu.github.io` أو من نطاق مخصص تملكه.
3. إذا وُزّعت نسخة مختلفة التوقيع عبر Google Play، أضف بصمة **App signing certificate** من Play Console إلى القائمة أيضًا.
4. بعد نشر الملف، افتح التطبيق المثبت من أيقونته. تثبيت PWA من الموقع يظل يعمل مستقلًا عند فتح الأيقونة؛ أما فتح الرابط في تبويب عادي فسيظل داخل المتصفح.

لا تعدّل شهادة APK الحالية ولا تعِد توقيعها بمفتاح مختلف من غير إضافة بصمته إلى الملف.
