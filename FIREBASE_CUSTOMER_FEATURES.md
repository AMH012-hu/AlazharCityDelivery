# تشغيل ساعات المتجر وإشعارات العملاء

## ساعات الفتح والقفل

- الافتراضي: كل يوم من 06:00 صباحًا إلى 14:00 (2 ظهرًا)، بتوقيت القاهرة.
- حساب الإدارة يغيّر الوقت من «بوابة التشغيل» ← «إعدادات الموقع».
- «المتجر مقفول النهارده» يقفل استقبال الطلبات لهذا التاريخ فقط، ثم يرجع الجدول الطبيعي تلقائيًا في اليوم التالي.
- الموقع يمنع إرسال الطلب عندما يكون المتجر مغلقًا.

## إضافة المتاجر وقوائمها

من إدارة المنتجات اختار المتجر والقسم واكتب السعر والصورة. السوبر ماركت وصيدلية الجهيني ظاهران بمنتجاتهما الحالية؛ كشري هند ومخبز الحي السادس وبيتزا الرحمة يظهرون كـ«قريبًا» لحد إضافة منتجاتهم وأسعارهم. كل سلة وطلب مرتبط بمتجر واحد.

## تفعيل إشعارات العملاء

1. من Firebase Console افتح إعدادات المشروع ثم Cloud Messaging وأنشئ شهادة Web Push. استخدم **المفتاح العام فقط**.
2. من بوابة الإدارة افتح «إعدادات الموقع»، الصق المفتاح العام في خانته واحفظ.
3. انشر قواعد Firestore والـ callable function من مجلد المشروع:

   ```sh
   firebase deploy --only firestore:rules,functions
   ```

4. العميل يسجّل الدخول بحساب مفعّل، يفتح صفحة الإشعارات ويضغط «فعّل إشعارات العروض» ثم يوافق على طلب الجهاز.
5. من بوابة الإدارة ← «إشعارات العملاء» اكتب عنوانًا ورسالة قصيرة وأرسلها. الإرسال يصل للأجهزة التي وافقت فقط، ويحتاج مشروع Firebase على خطة تسمح بـCloud Functions.

لا تضع مفتاح خدمة Firebase أو مفتاحًا خاصًا في صفحة العميل. مفتاح Web Push العام وحده يُحفظ ضمن إعدادات الموقع؛ إرسال البث نفسه يجري من Cloud Function بعد التحقق من صلاحية الإدارة.

## شكل التطبيق

الصفحة تستخدم PWA بوضع `standalone`: بعد تثبيتها من شاشة الهاتف تفتح في نافذة مستقلة بلا شريط المتصفح. فتح الرابط مباشرة في تبويب يظل داخل المتصفح. مشهد الافتتاح رسم SVG متحرك خفيف، وليس ملف فيديو MP4.
